# シェル実装を通じたOS学習記録

このドキュメントは、最小限のUnixシェルを実装しながら学んだOSの基礎概念をまとめたものです。

## 目次

1. [プロセスモデルの基礎](#1-プロセスモデルの基礎)
2. [fork() - プロセスの複製](#2-fork---プロセスの複製)
3. [exec() - プログラムの実行](#3-exec---プログラムの実行)
4. [wait() - 子プロセスの待機](#4-wait---子プロセスの待機)
5. [コマンドのパース](#5-コマンドのパース)
6. [ファイルディスクリプタ](#6-ファイルディスクリプタ)
7. [リダイレクション](#7-リダイレクション)
8. [パイプ](#8-パイプ)
9. [値渡しとポインタ渡し](#9-値渡しとポインタ渡し)

---

## 1. プロセスモデルの基礎

### プロセスとは

**プロセス = 実行中のプログラム**

- プログラム：ディスク上の静的なファイル
- プロセス：メモリ上で動いている動的な実体

### プロセスが持つもの

```
┌─────────────────────────┐
│ プロセス (PID=1234)      │
├─────────────────────────┤
│ メモリ空間               │
│  - コード領域            │
│  - データ領域            │
│  - スタック              │
│  - ヒープ                │
├─────────────────────────┤
│ レジスタ                 │
│  - PC (プログラムカウンタ)│
│  - SP (スタックポインタ)  │
├─────────────────────────┤
│ ファイルディスクリプタ    │
│  - FD 0: stdin          │
│  - FD 1: stdout         │
│  - FD 2: stderr         │
├─────────────────────────┤
│ 環境変数                 │
│ カレントディレクトリ      │
│ ユーザーID               │
└─────────────────────────┘
```

---

## 2. fork() - プロセスの複製

### 関数定義

```c
pid_t fork(void);
```

### 動作

**現在のプロセスの完全なコピーを作成する**

```
[親プロセス PID=1000]
    |
    | fork() 呼び出し
    |
    +─────────────+
    |             |
[親 PID=1000]  [子 PID=1001]
戻り値 = 1001   戻り値 = 0
(子のPID)      (常に0)
```

### 戻り値

- **親プロセス**: 子プロセスのPID（正の整数）
- **子プロセス**: 0
- **エラー**: -1

### 何がコピーされるか

- メモリ空間（コード、データ、スタック、ヒープ）
- レジスタの値
- ファイルディスクリプタ
- 環境変数
- カレントディレクトリ

### 何がコピーされないか

- プロセスID（子は新しいPIDを持つ）
- 親プロセスのPID（子は自分の親のPIDを持つ）
- 一部のロックやタイマー

### 重要な概念：逐次的ではない

```c
printf("A\n");           // 1つのプロセスが実行
pid_t pid = fork();      // ← ここで2つに分岐！
printf("B\n");           // 2つのプロセスが実行

// 出力:
// A
// B  ← 親が出力
// B  ← 子も出力
```

**プログラムを上から下に読むだけでは理解できない！**

fork() 以降は、2つのプロセスが並行して実行される。

### 典型的なパターン

```c
pid_t pid = fork();

if (pid == 0) {
    // 子プロセスのコード
    printf("I'm child, PID=%d\n", getpid());
} else if (pid > 0) {
    // 親プロセスのコード
    printf("I'm parent, child PID=%d\n", pid);
} else {
    // エラー処理
    perror("fork");
}
```

---

## 3. exec() - プログラムの実行

### 関数ファミリー

```c
int execvp(const char *file, char *const argv[]);
int execv(const char *path, char *const argv[]);
int execl(const char *path, const char *arg, ...);
```

### 動作

**現在のプロセスのメモリ空間を新しいプログラムで上書きする**

```
実行前:
┌─────────────┐
│ mysh        │
│ (シェル)     │
└─────────────┘

execvp("ls", ["ls", "-la", NULL])

実行後:
┌─────────────┐
│ ls          │
│ (新プログラム)│
└─────────────┘
```

### 重要なポイント

1. **新しいプロセスは作らない** - 既存のプロセスを置き換える
2. **成功すると戻ってこない** - プロセス自体が別物になるため
3. **失敗したときだけ戻る** - 戻り値は -1

### fork() と組み合わせる理由

```c
// シェルが ls を実行する例

pid_t pid = fork();  // まず子プロセスを作る

if (pid == 0) {
    // 子プロセスだけが exec する
    execvp("ls", args);
    // 成功したらここには来ない
    perror("execvp");  // 失敗時のみ
    exit(1);
}

// 親（シェル）はそのまま動き続ける
wait(NULL);  // 子の終了を待つ
```

**なぜ fork してから exec？**
- シェル自身を上書きしたくないから
- 子プロセスを上書きして、新しいプログラムを実行させる

---

## 4. wait() - 子プロセスの待機

### 関数定義

```c
pid_t wait(int *status);
pid_t waitpid(pid_t pid, int *status, int options);
```

### 動作

**子プロセスの終了を待つ**

```
時刻  親プロセス              子プロセス
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
t0   fork()
t1   wait() 呼び出し
t2   (ブロック - 待機中)     実行中
t3   (ブロック - 待機中)     実行中
t4   (ブロック - 待機中)     終了
t5   wait() から戻る
t6   次の処理を実行
```

### なぜ必要か

**wait() しないと子プロセスがゾンビになる**

```
親が wait() しない場合:
┌──────────┐
│ 親       │  ← 実行中
└──────────┘
    |
┌──────────┐
│ 子(ゾンビ)│  ← 終了したが、リソースが解放されていない
└──────────┘
```

**ゾンビプロセス:**
- 既に終了しているが、プロセステーブルにエントリが残っている
- メモリは解放されているが、PIDは占有されたまま
- 親が wait() を呼ぶまで残り続ける

---

## 5. コマンドのパース

### 目的

文字列を引数の配列に分割する

```
入力: "ls -la /tmp"
出力: ["ls", "-la", "/tmp", NULL]
```

### 実装

```c
int parse_command(char *line, command_t *cmd) {
    cmd->argc = 0;
    char *token = strtok(line, " \t");  // 空白とタブで分割

    while (token != NULL) {
        cmd->args[cmd->argc] = token;  // ポインタを格納
        cmd->argc++;
        token = strtok(NULL, " \t");   // 次のトークン
    }

    cmd->args[cmd->argc] = NULL;  // exec 用の終端
    return 0;
}
```

### strtok の注意点

1. **元の文字列を破壊する** - 区切り文字を `\0` に置き換える
2. **内部で状態を保持** - 2回目以降は `NULL` を渡す
3. **スレッドセーフではない**

### メモリ構造

```
line バッファ:
"ls -la /tmp"
 ↓ strtok 実行後
"ls\0-la\0/tmp\0"

cmd->args:
[0] → "ls"    (line のアドレス)
[1] → "-la"   (line + 3 のアドレス)
[2] → "/tmp"  (line + 7 のアドレス)
[3] = NULL
```

**重要:** args に格納されるのは**ポインタ**であり、文字列のコピーではない。

---

## 6. ファイルディスクリプタ

### 概念

**ファイルディスクリプタ（FD）= 開いているファイルを識別する整数**

```
プロセスのFDテーブル:
┌────┬──────────┐
│ 0  │ → stdin  │
│ 1  │ → stdout │
│ 2  │ → stderr │
│ 3  │ → file.txt│
└────┴──────────┘
  ↑
整数（インデックス）
```

### カーネルの管理

```
ユーザー空間:              カーネル空間:
┌──────────┐             ┌─────────────────┐
│ int fd=3 │ ─────→      │ ファイルテーブル  │
└──────────┘  インデックス │ [3] → /tmp/out  │
                         └─────────────────┘
                                  ↓
                         ┌─────────────────┐
                         │ 実際のファイル    │
                         │ /tmp/out        │
                         └─────────────────┘
```

### ポインタとの違い

| 項目 | ポインタ | ファイルディスクリプタ |
|------|---------|---------------------|
| 型 | メモリアドレス | 整数（インデックス） |
| スコープ | プログラムのメモリ空間 | OSカーネルが管理 |
| アクセス方法 | 直接メモリアクセス | システムコール経由 |
| 例 | `*ptr` | `read(fd, ...)` |

---

## 7. リダイレクション

### 概念

**ファイルディスクリプタの付け替え**

```
通常:
ls プロセス
FD 1 (stdout) → 画面

リダイレクション後 (ls > output.txt):
ls プロセス
FD 1 (stdout) → output.txt
```

### dup2() システムコール

```c
int dup2(int oldfd, int newfd);
```

**動作:**
1. newfd が開いていたら閉じる
2. newfd を oldfd のコピーにする
3. newfd を返す

### 実装の流れ

```c
// ls > output.txt の実装

int fd = open("output.txt", O_WRONLY | O_CREAT | O_TRUNC, 0644);
dup2(fd, 1);  // FD 1 (stdout) を output.txt に向ける
close(fd);    // 元の fd は不要

execvp("ls", args);  // ls の出力は output.txt に
```

### メモリ図解

```
実行前:
プロセスのFDテーブル:
┌────┬──────────┐
│ 0  │ → stdin  │
│ 1  │ → stdout │ ← これを変更したい
│ 2  │ → stderr │
└────┴──────────┘

open("output.txt") → fd = 3:
┌────┬──────────────┐
│ 0  │ → stdin      │
│ 1  │ → stdout     │
│ 2  │ → stderr     │
│ 3  │ → output.txt │ ← 新しく開いた
└────┴──────────────┘

dup2(3, 1) 実行後:
┌────┬──────────────┐
│ 0  │ → stdin      │
│ 1  │ → output.txt │ ← 変更された！
│ 2  │ → stderr     │
│ 3  │ → output.txt │ ← 同じファイル
└────┴──────────────┘

close(3) 実行後:
┌────┬──────────────┐
│ 0  │ → stdin      │
│ 1  │ → output.txt │ ← これだけ残る
│ 2  │ → stderr     │
└────┴──────────────┘
```

### リダイレクションの種類

| 記号 | 意味 | open のフラグ |
|------|------|--------------|
| `>` | 出力（上書き） | `O_WRONLY \| O_CREAT \| O_TRUNC` |
| `>>` | 出力（追記） | `O_WRONLY \| O_CREAT \| O_APPEND` |
| `<` | 入力 | `O_RDONLY` |

---

## 8. パイプ

### 概念

**2つのプロセスをつなぐデータの管**

```
ls | grep main

┌────────┐    パイプ    ┌──────────┐
│   ls   │ ─────────→  │   grep   │
└────────┘             └──────────┘
  stdout                 stdin
```

### pipe() システムコール

```c
int pipe(int pipefd[2]);
```

**動作:**
- 2つのファイルディスクリプタを作成
- `pipefd[0]`: 読み取り用（read end）
- `pipefd[1]`: 書き込み用（write end）

### パイプの実体

**カーネル内のメモリバッファ（通常64KB）**

```
プロセスのFDテーブル:
┌────┬─────────────┐
│ 3  │ → パイプ[読] │ ← pipefd[0]
│ 4  │ → パイプ[書] │ ← pipefd[1]
└────┴─────────────┘
       ↑         ↓
       └────┬────┘
     カーネル内のバッファ
     ┌─┬─┬─┬─┬─┐
     │H│e│l│l│o│
     └─┴─┴─┴─┴─┘
```

### パイプの実装パターン

```c
// ls | grep main の実装

int pipefd[2];
pipe(pipefd);  // パイプ作成

// 子プロセス1（ls）
if (fork() == 0) {
    close(pipefd[0]);       // 読み取り側を閉じる
    dup2(pipefd[1], 1);     // stdout をパイプに
    close(pipefd[1]);
    execvp("ls", ...);
}

// 子プロセス2（grep）
if (fork() == 0) {
    close(pipefd[1]);       // 書き込み側を閉じる
    dup2(pipefd[0], 0);     // stdin をパイプから
    close(pipefd[0]);
    execvp("grep", ...);
}

// 親プロセス
close(pipefd[0]);  // 親は使わない
close(pipefd[1]);
wait(NULL);  // 子1の終了を待つ
wait(NULL);  // 子2の終了を待つ
```

### データの流れ

```
時刻  ls プロセス        パイプ          grep プロセス
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
t0   write("file1\n")
t1                     [file1\n]
t2                     [file1\n]       read() → "file1\n"
t3   write("main.c\n")
t4                     [main.c\n]
t5                     [main.c\n]      read() → "main.c\n"
t6   (終了)
t7                     (空)
t8                     EOF            read() → 0 (EOF)
```

### 重要な特性

#### 1. FIFO（First In, First Out）

```c
write(pipefd[1], "ABC", 3);
write(pipefd[1], "XYZ", 3);

read(pipefd[0], buf, 6);  // buf = "ABCXYZ"（順序保証）
```

#### 2. ブロッキング

- 空のパイプから read() → ブロック（データが来るまで待つ）
- 満杯のパイプに write() → ブロック（空きができるまで待つ）

#### 3. EOF

- すべての書き込み側FDが閉じられる → EOF
- read() が 0 を返す

### なぜ close() が重要か

```c
// 子プロセス1
close(pipefd[0]);  // 使わない読み取り側を閉じる

// 子プロセス2
close(pipefd[1]);  // 使わない書き込み側を閉じる

// 親プロセス
close(pipefd[0]);  // 両方閉じる
close(pipefd[1]);
```

**理由:**
1. **EOF の検出** - すべての書き込み側が閉じられないとEOFが送られない
2. **デッドロック防止** - 不要なFDが開いたままだとプロセスが終了を待ち続ける
3. **FDの節約** - 限られたリソースの有効活用

---

## 9. 値渡しとポインタ渡し

### 基本的な違い

#### 値渡し（Pass by Value）

```c
void func(int x) {
    x = 100;  // コピーを変更するだけ
}

int main() {
    int a = 42;
    func(a);
    printf("%d\n", a);  // 出力: 42（変わらない）
}
```

**メモリ:**
```
main: [a = 42]
func: [x = 42] ← コピー
```

#### ポインタ渡し（Pass by Pointer）

```c
void func(int *x) {
    *x = 100;  // 元の変数を変更
}

int main() {
    int a = 42;
    func(&a);
    printf("%d\n", a);  // 出力: 100（変わった）
}
```

**メモリ:**
```
main: [a = 42] ← ポインタ経由で変更される
func: [x = &a] ← アドレスをコピー
```

### 構造体の場合

#### 値渡し（非効率）

```c
int execute_command(command_t cmd) {
    // command_t 全体（数百バイト）がコピーされる
}
```

**メモリコピー:**
```
呼び出し側:
  command_t cmd (528バイト)
    ↓ コピー（遅い）
execute_command 内:
  command_t cmd (528バイト)
```

#### ポインタ渡し（効率的）

```c
int execute_command(command_t *cmd) {
    // ポインタ（8バイト）だけをコピー
}
```

**メモリコピー:**
```
呼び出し側:
  command_t cmd (528バイト)
    ↑
    │ ポインタ（8バイト）だけコピー
execute_command 内:
  command_t *cmd → 元のデータを指す
```

### 構造体のメンバーとして持つ場合

#### 値で持つ

```c
typedef struct {
    redirect_t redirect;  // 埋め込まれる
} command_t;

メモリ:
command_t:
┌────────────┐
│ args[64]   │
│ argc       │
│ redirect { │ ← 直接埋め込まれている
│   type     │
│   filename │
│ }          │
└────────────┘
```

**メリット:**
- シンプル（malloc/free 不要）
- 速い（ポインタ経由のアクセスより直接アクセスが速い）
- 安全（ダングリングポインタのリスクなし）

#### ポインタで持つ

```c
typedef struct {
    redirect_t *redirect;  // ポインタ
} command_t;

メモリ:
command_t:
┌────────────┐
│ args[64]   │
│ argc       │
│ redirect → │
└────────────┘
      ↓
redirect_t:
┌────────────┐
│ type       │
│ filename   │
└────────────┘
```

**メリット:**
- オプショナル（NULL で「なし」を表現）
- 動的サイズ

**デメリット:**
- メモリ管理が必要（malloc/free）
- NULL チェックが必要

---

## まとめ

### シェルの基本動作フロー

```
1. プロンプト表示
   ↓
2. ユーザー入力読み取り
   ↓
3. パース（コマンド + リダイレクション）
   ↓
4. fork() で子プロセス作成
   ↓
5. 子プロセスで:
   - リダイレクション設定（dup2）
   - パイプ設定（dup2）
   - execvp() でコマンド実行
   ↓
6. 親プロセスで:
   - wait() で子の終了を待つ
   ↓
7. 1に戻る
```

### 学んだシステムコール

| システムコール | 用途 | 戻り値 |
|--------------|------|--------|
| `fork()` | プロセス複製 | 親:子PID, 子:0 |
| `exec()` | プログラム実行 | 成功すると戻らない |
| `wait()` | 子プロセス待機 | 子PID |
| `open()` | ファイルを開く | FD |
| `close()` | FDを閉じる | 0/-1 |
| `dup2()` | FD複製 | newfd |
| `pipe()` | パイプ作成 | 0/-1 |
| `read()` | データ読み取り | 読み取りバイト数 |
| `write()` | データ書き込み | 書き込みバイト数 |

### 重要な概念

1. **プロセスは独立したメモリ空間を持つ**
2. **fork() で並行実行が始まる** - 逐次的な思考では理解できない
3. **ファイルディスクリプタはカーネルが管理** - ポインタとは違う
4. **リダイレクション = FDの付け替え** - プログラム自体は変わらない
5. **パイプ = カーネル内のバッファ** - ファイルではない
6. **値渡しとポインタ渡しを使い分ける** - 効率と意図の明確化

---

## 参考資料

- [fork(2) - Linux manual page](https://man7.org/linux/man-pages/man2/fork.2.html)
- [exec(3) - Linux manual page](https://man7.org/linux/man-pages/man3/exec.3.html)
- [pipe(2) - Linux manual page](https://man7.org/linux/man-pages/man2/pipe.2.html)
- [dup2(2) - Linux manual page](https://man7.org/linux/man-pages/man2/dup2.2.html)
- 『詳解UNIXプログラミング』（APUE）
